# Demeter

Demeter is a webapp which allows users to share their favorite recipes.

The backend is written in Rust using [Rocket](https://rocket.rs/), the frontend is written in JavaScript using [React](https://reactjs.org/).